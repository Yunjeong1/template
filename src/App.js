//불러올때는 비구조화 할당을 이용해서 원하는 컴포넌트만 선별적으로 import 가능
import { Header, Popup } from './Common';

function App() {
	return (
		<div className='App'>
			<h1>새로운 컴포넌트 만들기</h1>
		</div>
	);
}

export default App;
